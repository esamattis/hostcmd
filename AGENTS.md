
Ensure all functions, structs, constant, enums and enum variants have descriptive doc comments.

All assert staments in test functions should include a descriptive message.

Ensure the README.md is up-to-date with the latest changes when modifying the CLI args or environment variables.

After *.rs changes run ./scripts/test.sh
